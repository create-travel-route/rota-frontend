import * as React from 'react';
import Button from '@mui/material/Button';

export default function SecondaryButton() {
  return (
    <div>
      <Button className="secondaryButton" variant="outlined">
        Secondary Button
      </Button>
    </div>
  );
}
